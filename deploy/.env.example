# =============================================================================
# SEO Articles Production Environment Configuration
# =============================================================================
#
# IMPORTANT: Copy this file to .env and fill in the values
#            ./setup.sh will auto-generate secrets for you
#
# =============================================================================

# === Docker Hub ===
# Your Docker Hub username (images will be pulled from here)
DOCKERHUB_USERNAME=makar0n1

# === Domains ===
# These are used for Grafana ROOT_URL and documentation
# Actual routing is configured in Nginx Proxy Manager
FRONTEND_DOMAIN=app.yourdomain.com
API_DOMAIN=api.yourdomain.com
GRAFANA_DOMAIN=grafana.yourdomain.com

# === Security ===
# IMPORTANT: These will be auto-generated by setup.sh
# If setting manually, use: openssl rand -hex 64 (for JWT_SECRET)
# and: openssl rand -hex 32 (for ENCRYPTION_KEY)
JWT_SECRET=generate-with-openssl-rand-hex-64
JWT_EXPIRES_IN=14d
ENCRYPTION_KEY=generate-with-openssl-rand-hex-32

# === Worker Scaling ===
# Concurrency: how many jobs each worker processes simultaneously
# Replicas: how many worker containers to run
WORKER_CONCURRENCY=2
WORKER_REPLICAS=3

# === Grafana ===
# Admin credentials for Grafana dashboard
GRAFANA_USER=admin
GRAFANA_PASSWORD=your-secure-grafana-password
