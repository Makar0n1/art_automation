# Server Configuration
PORT=3001
NODE_ENV=development

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/seo_articles

# Redis Connection (for Bull Queue)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=14d

# Encryption Key for API Keys (optional, 64 hex chars = 32 bytes)
# If not set, key will be derived from JWT_SECRET
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# Queue Configuration
MAX_CONCURRENT_GENERATIONS=5

# Worker Configuration (for docker workers)
WORKER_CONCURRENCY=2

# ===========================================
# IMPORTANT: User Setup
# ===========================================
# No default credentials are configured.
# Run the setup script to create your admin user:
#   npm run setup:user
#
# This will prompt for:
#   - Email
#   - Password (min 6 chars)
#   - PIN (4-8 digits, for API key protection)
# ===========================================
